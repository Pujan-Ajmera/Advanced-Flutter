import '../../utils/import_export.dart';

class Questions extends StatelessWidget {
  final String? name;
  const Questions({super.key,this.name});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
